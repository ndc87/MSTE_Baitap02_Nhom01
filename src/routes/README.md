# Routes
Thư mục này định nghĩa các đường dẫn API (Endpoints) của ứng dụng.
Mỗi file route sẽ gom nhóm các API liên quan (VD: `userRoutes.js`, `productRoutes.js`) và chuyển tiếp xử lý tới Controller tương ứng.
